name: SAST Evaluation Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  security-scan:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run SAST Scans (Semgrep & Trivy)
        shell: powershell
        run: |
          .\scripts\run_tools_local.ps1 -Tool all
