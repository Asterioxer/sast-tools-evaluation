# Klocwork – Linux Commands

## Installation
```bash
./kw-server-installer.sh
```

## Basic Scan
```bash
kwcheck create -u user -b build_spec
kwcheck run
```

## Report Generation
```bash
# Reports are managed via the Portal or generated via CLI commands (kwcheck list -F detailed).
# The scan command captures the defects.
```
